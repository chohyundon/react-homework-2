const Footer = ({ title }) => (
  <footer style={{ paddingLeft: "14px", paddingTop: "35px" }}>
    <form>
      <button type="button" style={{ display: "flex" }}>
        작성완료
      </button>
    </form>
  </footer>
);

export default Footer;
